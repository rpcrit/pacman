apiVersion: apps/v1
    spec:
      containers:
      - image: hashicorp/http-echo
        name: http-echo
        ports:
        - containerPort: 5678
          protocol: TCP
        resources:
          requests:
            cpu: 10m
